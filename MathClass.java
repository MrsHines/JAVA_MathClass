public static class MathClass
{
    // ALL of your method headers should follow the pattern: public static <return type> <name>(<parameters>)
    // This is because we are not writing a constructor for this class.  It is not meant to be replicated as objects but meant to be used
    // as functions similar to the Math class.
    
}
